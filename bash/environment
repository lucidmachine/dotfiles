# PATH
export PATH="${HOME}/bin:$PATH"

# Toolchains
export NVM_DIR="${HOME}/.nvm"
source_if_exists "${NVM_DIR}/nvm.sh"

# Machine-specific environment extensions
source_if_exists "${HOME}/.environment-ext"
