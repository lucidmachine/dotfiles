- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bash_aliases: bash/bash_aliases
    ~/.bashrc: bash/bashrc
    ~/.palette: bash/palette
    ~/.profile: bash/profile
    ~/.vim: vim
    ~/.vimrc: vim/vimrc
